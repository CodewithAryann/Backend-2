<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="/stylesheets/stylesheet.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-10">
    <div class="w-full max-w-md mx-auto">
        <a class="text-blue-500 hover:underline" href="/read">&lt; Back to Users</a>
        <h1 class="text-3xl font-bold tracking-tight mb-6 text-center">Edit User</h1>
        <form action="/edit/<%= user._id %>" method="post" class="space-y-4 bg-zinc-800 p-6 rounded-lg shadow-md">
            <div>
                <input class="w-full px-5 py-3 border-2 border-zinc-600 rounded-md bg-transparent focus:border-blue-500 focus:outline-none" 
                       type="text" name="name" value="<%= user.name %>" placeholder="Name" required>
            </div>
            <div>
                <input class="w-full px-5 py-3 border-2 border-zinc-600 rounded-md bg-transparent focus:border-blue-500 focus:outline-none" 
                       type="email" name="email" value="<%= user.email %>" placeholder="Email" required>
            </div>
            <div>
                <input class="w-full px-5 py-3 border-2 border-zinc-600 rounded-md bg-transparent focus:border-blue-500 focus:outline-none" 
                       type="text" name="image" value="<%= user.image %>" placeholder="Image URL" required>
            </div>
            <div class="text-center">
                <input class="px-5 py-2 bg-blue-500 rounded-lg cursor-pointer hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400" 
                       type="submit" value="Update User">
            </div>
        </form>
    </div>
</body>
</html>
