<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <link rel="stylesheet" href="/stylesheets/stylesheet.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen p-10">
    <div class="container mx-auto">
        <a class="text-blue-500 hover:underline" href="/">&lt; Back To Home</a>
        <h1 class="text-3xl font-bold tracking-tight mb-5 mt-5">All Users</h1>
        <div class="users flex gap-6 flex-wrap justify-center pb-10">
            <% if (users.length > 0) { %>
                <% users.forEach(function(user) { %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg shadow-md">
                        <div class="w-full h-72 rounded-lg overflow-hidden bg-zinc-700">
                            <img class="w-full h-full object-cover object-top" src="<%= user.image %>" alt="User Image">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tight"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-4 mt-4">
                            <a class="text-blue-500 hover:underline" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-500 hover:underline" href="/delete/<%= user._id %>">Delete</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <h3 class="text-center text-xl text-zinc-500">No Users</h3>
            <% } %>
        </div>
    </div>
</body>
</html>
